<h3>Answer this question</h3>

<%= form_for([@question, @question.answers.build]) do |f| %>

	<p>
		<%= f.label :body, "Answer" %><br/>
		<%= f.text_area(:body, {:class => 'form-control'}) %>
	</p>

	<p>
		<%= f.submit({:class => 'btn btn-default', :value => 'Submit'}) %>
	</p>
<% end %>